/// <summary>
/// Stores the Type of the space above the cell grid.
/// EmptyCell,Player,Enemy,Obstruction are the only valid values
/// </summary>
public enum CellType {
    EmptyCell,
    Player,
    Enemy,
    Obstruction
}